// React
import React from 'react';

const ErrorMsg = () => {
  return (
    <p>
      Sorry the page you are looking for can't be locate or the link is broken
    </p>
  );
};

export default ErrorMsg;
